<div class="banner mb-10" style="background-image: linear-gradient(rgba(0,0,0,0.2),rgba(0,0,0,0.2)), url(<%= image_path "nid-cigognes.jpg"%>); background-position: center top;">
  <div class="container">
    <h1>Woof & Roof</h1>
    <p>Plus d'un logement, une expérience.</p>
    <%= link_to "Proposer un logement", new_offer_path, class: "btn btn-ghost" %>
  </div>
</div>


<div class="container mt-5 mb-10">
  <h1 class = "text-center mb-2" >Créez votre annonce</h1>
  <div class="row">
    <div class="col-6 mb-2">
    <%= simple_form_for(@offer) do |f| %>
      <%= f.input :title, label: "Titre" %>
      <%= f.input :description %>
      <%= f.input :price, label: "Prix" %>
      <%= f.input :address, label:"Adresse" %>
      <%= f.input :photos, as: :file, input_html: { multiple: true } %>
      <div class="text-end mt-4">
        <%= f.button :submit, "Envoyer",class: "button_general" %>
      </div>
    <% end %>
    </div>
    <div class="col-md-6 text-center mt-4 mb-5">
      <img src="<%= asset_path('hibou.jpeg') %>" alt="Hibou" class="img-fluid rounded" style="max-height: 600px; width: auto;">
    </div>
  </div>
</div>
